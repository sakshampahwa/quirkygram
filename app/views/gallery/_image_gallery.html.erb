<table>
    <tr>
        <% count=0 %>
        <% @gallery.each do |gallery|%>
        <% if (count % 3) == 0%>
    </tr>
    <tr>
        <% end %>
        <% count+=1 %>
        <td><br>
            <% if gallery.images.attached? %>
                <%= image_tag gallery.images.first, style:'width:250px; height: 250px'%>
            <% end %><br><br>
            <strong><%= gallery.name %></strong>
            <br /><strong>Price</strong> : ₹<%= gallery.price %>
            <br /><strong>Description</strong>: <%= gallery.description %>
            <br /><strong>Category:</strong> <%= gallery.category %><br><br>
            <%= button_to 'Add to cart',line_items_path(:store_id => gallery.id)%>
        </td>
        <% end %>
    </tr>
</table><br>